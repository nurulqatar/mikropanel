<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">

    <title>{{ $documentTitle ?? 'Invoice' }}</title>

    <style>
        @page {
            size: A4 portrait;
            margin: 10mm;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            background: #eef2f7;
            color: #1e293b;
            font-family: DejaVu Sans, Arial, sans-serif;
            font-size: 12px;
            line-height: 1.5;
        }

        .toolbar {
            max-width: 900px;
            margin: 20px auto;
            padding: 15px 18px;
            background: #ffffff;
            border: 1px solid #dbe3ec;
            border-radius: 10px;
            text-align: right;
        }

        .toolbar-title {
            float: left;
            padding-top: 8px;
            color: #334155;
            font-size: 16px;
            font-weight: bold;
        }

        .toolbar a,
        .toolbar button {
            display: inline-block;
            margin-left: 8px;
            padding: 10px 18px;
            border: 0;
            border-radius: 7px;
            background: #0891b2;
            color: #ffffff;
            font-family: inherit;
            font-size: 13px;
            font-weight: bold;
            text-decoration: none;
            cursor: pointer;
        }

        .toolbar .back-button {
            background: #475569;
        }

        .invoice-page {
            width: 100%;
            max-width: 900px;
            min-height: 267mm;
            margin: 0 auto 25px;
            padding: 14mm;
            background: #ffffff;
            border: 1px solid #dbe3ec;
        }

        .page-break {
            page-break-after: always;
        }

        .header-table,
        .details-table,
        .summary-layout {
            width: 100%;
            border-collapse: collapse;
        }

        .header-table td {
            vertical-align: top;
        }

        .brand-cell {
            width: 62%;
        }

        .invoice-title-cell {
            width: 38%;
            text-align: right;
        }

        .brand-mark {
            display: inline-block;
            width: 52px;
            height: 52px;
            margin-right: 12px;
            border-radius: 8px;
            background: #0891b2;
            color: #ffffff;
            font-size: 18px;
            font-weight: bold;
            line-height: 52px;
            text-align: center;
            vertical-align: top;
        }

        .brand-content {
            display: inline-block;
            max-width: 460px;
            vertical-align: top;
        }

        .company-name {
            margin: 0 0 5px;
            color: #0f172a;
            font-size: 20px;
            font-weight: bold;
        }

        .company-contact {
            margin: 2px 0;
            color: #64748b;
            font-size: 11px;
        }

        .invoice-heading {
            margin: 0;
            color: #0891b2;
            font-size: 31px;
            font-weight: bold;
            letter-spacing: 1px;
        }

        .invoice-number {
            margin-top: 4px;
            color: #334155;
            font-size: 13px;
            font-weight: bold;
        }

        .top-line {
            height: 4px;
            margin: 18px 0 22px;
            background: #0891b2;
        }

        .details-table {
            margin-bottom: 22px;
        }

        .details-table td {
            width: 50%;
            padding: 0 12px 0 0;
            vertical-align: top;
        }

        .details-table td:last-child {
            padding-right: 0;
            padding-left: 12px;
        }

        .info-box {
            min-height: 125px;
            padding: 15px;
            border: 1px solid #dbe3ec;
            border-radius: 8px;
            background: #f8fafc;
        }

        .section-label {
            margin-bottom: 9px;
            color: #0891b2;
            font-size: 10px;
            font-weight: bold;
            letter-spacing: 0.8px;
            text-transform: uppercase;
        }

        .client-name {
            margin-bottom: 5px;
            color: #0f172a;
            font-size: 16px;
            font-weight: bold;
        }

        .info-line {
            margin: 3px 0;
            color: #475569;
        }

        .label {
            color: #64748b;
        }

        .value {
            color: #1e293b;
            font-weight: bold;
        }

        .status {
            display: inline-block;
            margin-top: 4px;
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 10px;
            font-weight: bold;
            text-transform: uppercase;
        }

        .status-paid {
            background: #dcfce7;
            color: #166534;
        }

        .status-partial {
            background: #fef3c7;
            color: #92400e;
        }

        .status-unpaid,
        .status-overdue {
            background: #fee2e2;
            color: #991b1b;
        }

        .status-cancelled {
            background: #e2e8f0;
            color: #475569;
        }

        .items-table,
        .payments-table {
            width: 100%;
            margin-top: 8px;
            border-collapse: collapse;
        }

        .items-table th,
        .payments-table th {
            padding: 10px 9px;
            background: #0f172a;
            color: #ffffff;
            font-size: 10px;
            font-weight: bold;
            text-align: left;
            text-transform: uppercase;
        }

        .items-table td,
        .payments-table td {
            padding: 11px 9px;
            border-bottom: 1px solid #e2e8f0;
            vertical-align: top;
        }

        .items-table .number,
        .payments-table .number {
            text-align: right;
        }

        .service-title {
            color: #0f172a;
            font-size: 13px;
            font-weight: bold;
        }

        .service-description {
            margin-top: 3px;
            color: #64748b;
            font-size: 10px;
        }

        .summary-layout {
            margin-top: 20px;
        }

        .summary-layout td {
            vertical-align: top;
        }

        .notes-cell {
            width: 56%;
            padding-right: 24px;
        }

        .totals-cell {
            width: 44%;
        }

        .notes-box {
            min-height: 92px;
            padding: 13px;
            border: 1px solid #dbe3ec;
            border-radius: 7px;
            background: #f8fafc;
        }

        .totals-table {
            width: 100%;
            border-collapse: collapse;
        }

        .totals-table td {
            padding: 7px 9px;
            border-bottom: 1px solid #e2e8f0;
        }

        .totals-table .amount {
            text-align: right;
            font-weight: bold;
        }

        .totals-table .total-row td {
            padding-top: 11px;
            padding-bottom: 11px;
            border-bottom: 0;
            background: #0891b2;
            color: #ffffff;
            font-size: 14px;
            font-weight: bold;
        }

        .payment-title {
            margin: 24px 0 6px;
            color: #0f172a;
            font-size: 14px;
            font-weight: bold;
        }

        .signature-table {
            width: 100%;
            margin-top: 45px;
            border-collapse: collapse;
        }

        .signature-table td {
            width: 50%;
            padding-top: 24px;
            vertical-align: bottom;
        }

        .signature-table td:last-child {
            text-align: right;
        }

        .signature-line {
            display: inline-block;
            width: 190px;
            padding-top: 7px;
            border-top: 1px solid #64748b;
            color: #64748b;
            font-size: 10px;
            text-align: center;
        }

        .footer {
            margin-top: 28px;
            padding-top: 12px;
            border-top: 1px solid #cbd5e1;
            color: #64748b;
            font-size: 10px;
            text-align: center;
        }

        .thank-you {
            margin-top: 12px;
            color: #0891b2;
            font-size: 13px;
            font-weight: bold;
            text-align: center;
        }

        @media print {
            body {
                background: #ffffff;
            }

            .toolbar {
                display: none;
            }

            .invoice-page {
                width: auto;
                max-width: none;
                min-height: 0;
                margin: 0;
                padding: 0;
                border: 0;
            }
        }

        /* DOMPDF_A4_WIDTH_FIX */
        .pdf-mode {
            width: 100%;
            margin: 0;
            padding: 0;
            background: #ffffff;
        }

        /*
         * PDF page-এর margin @page থেকে আসবে।
         * এখানে extra width/padding থাকবে না।
         */
        .pdf-mode .invoice-page {
            display: block;
            width: auto !important;
            max-width: none !important;
            min-height: 0 !important;
            margin: 0 !important;
            padding: 0 !important;
            border: 0 !important;
            overflow: visible !important;
        }

        .pdf-mode .header-table,
        .pdf-mode .details-table,
        .pdf-mode .items-table,
        .pdf-mode .payments-table,
        .pdf-mode .summary-layout,
        .pdf-mode .totals-table,
        .pdf-mode .signature-table {
            width: 100% !important;
            max-width: 100% !important;
            table-layout: fixed;
        }

        .pdf-mode td,
        .pdf-mode th,
        .pdf-mode .company-name,
        .pdf-mode .company-contact,
        .pdf-mode .client-name,
        .pdf-mode .info-line,
        .pdf-mode .service-description {
            word-wrap: break-word;
            overflow-wrap: break-word;
        }

        .pdf-mode .company-name {
            font-size: 18px;
        }

        .pdf-mode .company-contact {
            display: block;
            font-size: 10px;
        }

        .pdf-mode .invoice-heading {
            font-size: 27px;
        }

        .pdf-mode .brand-cell {
            width: 60%;
        }

        .pdf-mode .invoice-title-cell {
            width: 40%;
        }

        .pdf-mode .details-table td {
            width: 50%;
            padding-right: 6px;
        }

        .pdf-mode .details-table td:last-child {
            padding-right: 0;
            padding-left: 6px;
        }

        .pdf-mode .notes-cell {
            width: 54%;
            padding-right: 12px;
        }

        .pdf-mode .totals-cell {
            width: 46%;
        }

        .pdf-mode .items-table th,
        .pdf-mode .items-table td,
        .pdf-mode .payments-table th,
        .pdf-mode .payments-table td {
            padding-left: 6px;
            padding-right: 6px;
        }

        .pdf-mode .page-break {
            height: 0;
            margin: 0;
            padding: 0;
            page-break-after: always;
        }

    </style>
</head>

<body class="{{ $pdfMode ? 'pdf-mode' : 'screen-mode' }}">
@if (!$pdfMode)
    <div class="toolbar">
        <div class="toolbar-title">
            {{ $documentTitle ?? 'Invoice' }}
        </div>

        <a
            href="{{ route('invoices.index') }}"
            class="back-button"
        >
            Back to Invoices
        </a>

        <button
            type="button"
            onclick="window.print()"
        >
            Print
        </button>

        <div style="clear: both;"></div>
    </div>
@endif

@forelse ($invoices as $invoice)
    @php
        $payments = $paymentsByInvoice->get(
            $invoice->id,
            collect()
        );

        $status = strtolower(
            $invoice->status ?? 'unpaid'
        );

        $subTotal = (float) $invoice->amount;
        $discount = (float) $invoice->discount;
        $netAmount = max(0, $subTotal - $discount);
        $paidAmount = (float) $invoice->paid_amount;
        $dueAmount = (float) $invoice->due_amount;

        $client = $invoice->client;
        $package = $client?->package;
    @endphp

    <div class="invoice-page">
        <table class="header-table">
            <tr>
                <td class="brand-cell">
                    <span class="brand-mark">
                        GIT
                    </span>

                    <span class="brand-content">
                        <span class="company-name">
                            {{ $panelSettings['company_name'] ?? 'Genius Information Technology W.L.L' }}
                        </span>

                        <span class="company-contact">
                            Mobile: {{ $panelSettings['company_phone'] ?? '+974 6633 2403' }}
                        </span>

                        <span class="company-contact">
                            Email: {{ $panelSettings['company_email'] ?? 'qatarhighspeedwifi@gmail.com' }}
                        </span>
                    </span>
                </td>

                <td class="invoice-title-cell">
                    <div class="invoice-heading">
                        INVOICE
                    </div>

                    <div class="invoice-number">
                        {{ $invoice->invoice_no }}
                    </div>

                    <span class="status status-{{ $status }}">
                        {{ strtoupper($status) }}
                    </span>
                </td>
            </tr>
        </table>

        <div class="top-line"></div>

        <table class="details-table">
            <tr>
                <td>
                    <div class="info-box">
                        <div class="section-label">
                            Bill To
                        </div>

                        <div class="client-name">
                            {{ $client?->name ?? 'Unknown Client' }}
                        </div>

                        <div class="info-line">
                            <span class="label">
                                Client Code:
                            </span>

                            <span class="value">
                                {{ $client?->client_code ?? '-' }}
                            </span>
                        </div>

                        <div class="info-line">
                            <span class="label">
                                Phone:
                            </span>

                            <span class="value">
                                {{ $client?->phone ?: '-' }}
                            </span>
                        </div>

                        <div class="info-line">
                            <span class="label">
                                IP Address:
                            </span>

                            <span class="value">
                                {{ $client?->ip_address ?: '-' }}
                            </span>
                        </div>

                        <div class="info-line">
                            <span class="label">
                                Package:
                            </span>

                            <span class="value">
                                {{ $package?->name ?? '-' }}
                            </span>
                        </div>
                    </div>
                </td>

                <td>
                    <div class="info-box">
                        <div class="section-label">
                            Invoice Details
                        </div>

                        <div class="info-line">
                            <span class="label">
                                Invoice Date:
                            </span>

                            <span class="value">
                                {{
                                    $invoice->issue_date
                                        ? \Carbon\Carbon::parse(
                                            $invoice->issue_date
                                        )->format('d M Y')
                                        : '-'
                                }}
                            </span>
                        </div>

                        <div class="info-line">
                            <span class="label">
                                Due Date:
                            </span>

                            <span class="value">
                                {{
                                    $invoice->due_date
                                        ? \Carbon\Carbon::parse(
                                            $invoice->due_date
                                        )->format('d M Y')
                                        : '-'
                                }}
                            </span>
                        </div>

                        <div class="info-line">
                            <span class="label">
                                Billing Period:
                            </span>

                            <span class="value">
                                {{
                                    $invoice->billing_month
                                        ? \Carbon\Carbon::parse(
                                            $invoice->billing_month
                                        )->format('F Y')
                                        : '-'
                                }}
                            </span>
                        </div>

                        <div class="info-line">
                            <span class="label">
                                Currency:
                            </span>

                            <span class="value">
                                QAR
                            </span>
                        </div>
                    </div>
                </td>
            </tr>
        </table>

        <table class="items-table">
            <thead>
                <tr>
                    <th style="width: 50%;">
                        Description
                    </th>

                    <th style="width: 12%;">
                        Qty
                    </th>

                    <th style="width: 19%; text-align: right;">
                        Unit Price
                    </th>

                    <th style="width: 19%; text-align: right;">
                        Amount
                    </th>
                </tr>
            </thead>

            <tbody>
                <tr>
                    <td>
                        <div class="service-title">
                            Internet Service
                        </div>

                        <div class="service-description">
                            {{ $package?->name ?? 'Internet Package' }}

                            @if ($invoice->billing_month)
                                -
                                {{
                                    \Carbon\Carbon::parse(
                                        $invoice->billing_month
                                    )->format('F Y')
                                }}
                            @endif
                        </div>
                    </td>

                    <td>
                        1
                    </td>

                    <td class="number">
                        QAR {{ number_format($subTotal, 2) }}
                    </td>

                    <td class="number">
                        QAR {{ number_format($subTotal, 2) }}
                    </td>
                </tr>
            </tbody>
        </table>

        <table class="summary-layout">
            <tr>
                <td class="notes-cell">
                    <div class="notes-box">
                        <div class="section-label">
                            Notes
                        </div>

                        <div>
                            {{
                                $invoice->notes
                                    {{ $invoice->notes ?: 'Thank you for choosing ' . ($panelSettings['company_name'] ?? 'Genius Information Technology W.L.L') . '.' }}
                        </div>
                    </div>
                </td>

                <td class="totals-cell">
                    <table class="totals-table">
                        <tr>
                            <td>
                                Subtotal
                            </td>

                            <td class="amount">
                                QAR {{ number_format($subTotal, 2) }}
                            </td>
                        </tr>

                        <tr>
                            <td>
                                Discount
                            </td>

                            <td class="amount">
                                QAR {{ number_format($discount, 2) }}
                            </td>
                        </tr>

                        <tr>
                            <td>
                                Paid
                            </td>

                            <td class="amount">
                                QAR {{ number_format($paidAmount, 2) }}
                            </td>
                        </tr>

                        <tr class="total-row">
                            <td>
                                Balance Due
                            </td>

                            <td class="amount">
                                QAR {{ number_format($dueAmount, 2) }}
                            </td>
                        </tr>
                    </table>
                </td>
            </tr>
        </table>

        @if ($payments->isNotEmpty())
            <div class="payment-title">
                Payment History
            </div>

            <table class="payments-table">
                <thead>
                    <tr>
                        <th>
                            Date
                        </th>

                        <th>
                            Method
                        </th>

                        <th>
                            Transaction ID
                        </th>

                        <th style="text-align: right;">
                            Amount
                        </th>
                    </tr>
                </thead>

                <tbody>
                    @foreach ($payments as $payment)
                        <tr>
                            <td>
                                {{
                                    $payment->payment_date
                                        ? \Carbon\Carbon::parse(
                                            $payment->payment_date
                                        )->format('d M Y')
                                        : '-'
                                }}
                            </td>

                            <td>
                                {{ $payment->payment_method }}
                            </td>

                            <td>
                                {{ $payment->transaction_id ?: '-' }}
                            </td>

                            <td class="number">
                                QAR {{
                                    number_format(
                                        (float) $payment->amount,
                                        2
                                    )
                                }}
                            </td>
                        </tr>
                    @endforeach
                </tbody>
            </table>
        @endif

        <table class="signature-table">
            <tr>
                <td>
                    <span class="signature-line">
                        Customer Signature
                    </span>
                </td>

                <td>
                    <span class="signature-line">
                        Authorized Signature
                    </span>
                </td>
            </tr>
        </table>

        <div class="thank-you">
            Thank you for your business
        </div>

        <div class="footer">
            {{ $panelSettings['company_name'] ?? 'Genius Information Technology W.L.L' }} |
            Mobile: {{ $panelSettings['company_phone'] ?? '+974 6633 2403' }} |
            Email: {{ $panelSettings['company_email'] ?? 'qatarhighspeedwifi@gmail.com' }}
        </div>
    </div>

    @if (!$loop->last)
        <div class="page-break"></div>
    @endif
@empty
    <div class="invoice-page">
        <div style="padding: 80px; text-align: center;">
            <h2>No invoice found.</h2>
        </div>
    </div>
@endforelse

</body>
</html>
